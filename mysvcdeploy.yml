apiVersion: v1
kind: Service
metadata:
  name: mynginx-svc
  labels:
     app: nginx
     
spec:
  type: NodePort
  ports:
  - port: 80
    nodePort: 3001
    protocol: TCP
  selector:
      app:  nginx